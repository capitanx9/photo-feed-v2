[project]
name = "generate-image"
version = "0.1.0"
description = "generate_image package of photo-feed-v2"
readme = "README.md"
authors = [
    { name = "Kyrylo Triskalo", email = "capitanx9@gmail.com" }
]
requires-python = ">=3.12.10"
dependencies = [
    "boto3>=1.43",
]

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
